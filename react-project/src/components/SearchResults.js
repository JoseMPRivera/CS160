import React, { Component } from 'react';

export default class SearchResults extends Component {

  state = {
    query: '',
    results: []
  }

  render() {
    return (
      <div>
        {results.map()}
      </div>
    );
  }
}